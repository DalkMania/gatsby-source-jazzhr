export const createSchemaCustomization = async ({ actions }) => {
    const { createTypes } = actions;
};

export const sourceNodes = async ({ actions, getNode, store, cache, createNodeId, createContentDigest }) => {
    const { createNode } = actions;
};
